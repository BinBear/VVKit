source 'https://cdn.cocoapods.org/'
use_frameworks!

platform :ios, '10.0'

target 'VinBaseComponents_Example' do
  pod 'VinBaseComponents', :path => '../'

  target 'VinBaseComponents_Tests' do
    inherit! :search_paths

    
  end
end
